# Experiment runs

Training and evaluation outputs are excluded from Git. Runs should be
immutable and identify their configuration, dataset release, code revision,
metrics, logs, and resulting model artifacts.

