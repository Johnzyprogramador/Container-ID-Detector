# Model artifacts

Model weights are excluded from Git. Each model version should include metadata
identifying its architecture, dataset release, training configuration, code
revision, metrics, and exported runtime format.

