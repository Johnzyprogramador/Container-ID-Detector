# Local data

This directory is intentionally excluded from Git except for this file.

Recommended local or remote-machine layout:

```text
data/
├── raw/             immutable capture sessions
├── annotations/     editable master annotations
├── datasets/        immutable training releases
└── samples/         small non-sensitive development samples
```

